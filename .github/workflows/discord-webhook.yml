name: Discord Commit Notifier

on:
  push:
    branches: [ master ]  # ⚠️ Change to your branch name (e.g., main)

jobs:
  notify-discord:
    runs-on: ubuntu-latest
    steps:
      - name: Check if workflow runs
        run: echo "✅ Workflow triggered on $GITHUB_REF"
